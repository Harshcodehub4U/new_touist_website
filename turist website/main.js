const scrollRevealOption = {
    distance: "50px",
    origin: "bottom",
    duration: 1000,
  };
  
  ScrollReveal().reveal(".header-cont h1", {
    ...scrollRevealOption,
    reset: true

  });
  
  ScrollReveal().reveal(".header__form", {
    ...scrollRevealOption,
    delay: 500,
    reset: true

  });
0.

  ScrollReveal().reveal(".trending__card", {
    ...scrollRevealOption,
    interval: 500,
    reset: true

  });
  
  ScrollReveal().reveal(".destination__card", {
    duration: 1000,
    interval: 500,
    reset: true

  });
  
  ScrollReveal().reveal(".seller__card", {
    ...scrollRevealOption,
    interval: 500,
    reset: true

  });
  
  ScrollReveal().reveal(".guide__card", {
    ...scrollRevealOption,
    interval: 500,
    reset: true

  });
  
  ScrollReveal().reveal(".client__card", {
    ...scrollRevealOption,
    interval: 500,
    reset: true

  });

